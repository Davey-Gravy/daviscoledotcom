{{- /*
  Custom sitemap: identical to Hugo's built-in, except it EXCLUDES the gated
  `private` section by name. This is a version-proof safety net — it filters on
  .Section, which is correct on every Hugo version, so private URLs never reach
  the public sitemap even if the build/list cascade is ignored (e.g. an older
  Hugo on the deploy host). See content/private/_index.md.
*/ -}}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range where .Data.Pages "Section" "ne" "private" }}
    {{- if .Permalink }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
  </url>
    {{- end }}
  {{- end }}
</urlset>
